# DIBO 工程方案历次修改问题与冗余删减总结

## 1. 文档目的

本文总结原始 DIBO 工程规划在多轮讨论中暴露的问题、已经删除或简化的内容，以及当前版本最终保留的核心算法。

这不是新的完整实施方案，也不重复 15 维参数矩阵和完整 Action–Metric 图；这些内容以《DIBO 动态邻接 Action 贝叶斯调参方案 v8》为准。

---

## 2. 总体结论

原始方案的主要问题不是功能不够，而是：

1. 工程范围过大，混入了很多生产系统、分布式容错和故障恢复内容；
2. 部分建模结构与真正想实现的算法不一致；
3. Action、Metric、贝叶斯优化之间的选择关系不够准确；
4. 基准参数、动作叠加和 LLM 更新的规则不够明确；
5. 测试和模块数量过多，导致实现核心思想之前就需要完成大量外围代码。

修改后的方案把系统收缩为一条清楚的核心闭环：

> 初始实验 → 选择最佳基础样本 → 选择关键 Metric → 沿图取得全部相邻 Action → 只优化这些 Action → 从固定基础样本生成配置 → 执行实验 → 更新模型和动作向量。

---

## 3. 原方案中发现的主要问题

### 3.1 G 层的职责写错

原方案容易把 G 层理解为直接检测瓶颈，或把它用于选择参数和 Action。

正确职责是：

- F 层学习 `Action → Metric`；
- G 层学习 `Metrics → Throughput`；
- 系统先根据阈值找明显异常的 Metric；
- 如果没有明显异常，再利用 G 分析哪些 Metric 对吞吐最重要。

G 的作用是补充 Metric 重要性判断，而不是取代 F，也不是直接决定具体参数。

### 3.2 增加了没有必要的 H 模型或局部 GP

原方案曾加入额外的 `Action → Throughput` 模型，以及针对每一轮再建立一个局部 GP。

这些模型会造成：

- 与 F、G 的职责重复；
- 同一批有限样本被拆给更多模型；
- 系统结构更难解释；
- 偏离“先修复关键 Metric，再改善吞吐”的核心思想。

最终删除 H 和额外局部 GP，只保留：

- 每个 Metric 对其相邻 Action 的 F 模型；
- 一个 `Metrics → Throughput` 的 G 模型。

### 3.3 固定选择两个 Action 不符合算法

原方案曾把优化过程描述成“先选择 Top-2 Action，再做贝叶斯优化”。这与最终算法不一致。

正确做法是：

1. 每轮先选出 1～3 个关键 Metric，默认 2 个；
2. 在 Action–Metric 图中查找每个已选 Metric 的全部相邻 Action；
3. 对这些邻接集合求并集；
4. 并集中的所有 Action 一起进入本轮贝叶斯优化。

因此，Action 数量不是固定值，而是由当轮 Metric 和图结构共同决定，可能从 1 个变化到 8 个。

### 3.4 贝叶斯优化目标写偏

原方案更接近直接最大化吞吐，但当前系统的核心不是直接搜索吞吐，而是：

- 找到重要或异常的 Metric；
- 用 F 预测 Action 对这些 Metric 的影响；
- 优化相关 Action，使这些 Metric 接近正常范围；
- 吞吐主要通过 G 参与 Metric 重要性分析，并可作为候选点接近时的辅助比较依据。

因此，BO 的主目标应是选中 Metric 的归一化偏离损失，而不是额外建立一个直接吞吐优化层。

### 3.5 基础配置和增量关系不清楚

原方案没有严格规定后续候选配置应该基于哪一个样本生成，容易造成参数逐轮累加和漂移。

最终规则是：

- 先执行 5～10 次初始化实验；
- 从成功实验中选择吞吐最高的配置作为固定基础样本 `x_base`；
- 后续每个候选都从同一个 `x_base` 重新计算；
- 不能在上一个候选配置上继续累加。

即：

```text
x_candidate = x_base + 本轮已选 Action 产生的参数增量
```

这保证每个实验点都可以被准确复现和比较。

### 3.6 未选 Action 的处理方式错误

原方案对未选 Action 仍可能保留系数、参与扰动，或者仅依赖 Active Mask 排除。

最终规则是：

- 本轮由关键 Metric 的邻接关系确定 Action 集合；
- 集合外所有 Action 系数必须严格设为 `0.0`；
- 这些 Action 不进入本轮 BO 搜索空间，也不影响配置编译。

这里的 0 表示“本轮不参与”，不表示该 Action 被永久删除。

### 3.7 Active Mask 和参数冻结没有必要

Active Mask、能力 Mask、工作负载 Mask 和参数冻结会产生多套状态，并使“参数存在”“本轮是否被选中”“当前环境是否可行”混在一起。

最终全部删除：

- 不使用 Active Mask；
- 不冻结参数；
- 15 个参数、8 个 Action 始终保留在统一的数据结构中；
- 本轮是否参与优化只由 Metric→Action 邻接选择和 Action 系数是否为 0 决定；
- 硬件、范围和参数组合是否合法，交给统一的配置编译与约束检查处理。

### 3.8 “实测并冻结真正有效的 vLLM 参数”不合适

这句话原本想表达：有些参数在特定硬件、模型或 vLLM 版本中可能不生效，因此应该先验证。

但“冻结”会误导为永久排除参数，并与动态选择思想冲突。最终修改为：

- 编译候选配置时检查参数是否受当前环境支持；
- 记录实际生效的最终配置；
- 不建立永久冻结机制。

### 3.9 分类参数不能使用普通加法

布尔值和枚举值没有连续的数值距离，直接对它们做加法不合理。

最终采用加权投票：

- 每个 Action 在分类参数维度上的系数视为一张带方向和权重的票；
- 选中 Action 的票数加权求和；
- 布尔值超过阈值时切换；
- 有序枚举根据最终票值决定相对基础值的移动方向；
- 未选 Action 因系数为 0，不产生投票。

### 3.10 整数参数与连续搜索之间缺少转换规则

整数参数也可以由 BO 在连续空间中搜索，但最终必须经过确定性的编译过程：

1. 先计算连续增量；
2. 加到基础值；
3. 四舍五入；
4. 裁剪到合法范围；
5. 必要时对齐到参数要求的步长或倍数。

这使整数参数既能参与平滑搜索，又能生成合法的 vLLM 配置。

### 3.11 Action 矩阵中过多的 0 缺少方向信息

早期 Action 向量只给少数核心参数赋值，后几个 Action 中甚至大量为 0，不能充分表示一个有明确语义的调参方向。

最终重新设计 8×15 矩阵：

- 每个 Action 都有清楚的主要调节方向；
- 核心参数使用较大权重；
- 次要但有关联的参数使用小权重；
- 通过正负号表达增减方向；
- 只有确实无关的参数才保留为 0，当前初始化矩阵则尽量提供弱非零先验；
- LLM 后续可以根据实验结果进行小幅修正。

### 3.12 LLM 更新 Action 向量的时机不合理

如果 LLM 在每个实验点之后立即修改矩阵，BO 的搜索空间会不断变化，同一 Action 系数在不同实验中也不再表示同一种参数变化。

最终改为：

- 一个 BO 小轮次默认执行 3 个实验；
- 小轮次内部固定 Action 矩阵；
- 小轮次结束后，LLM 根据新结果修改下一轮矩阵；
- 每次修改幅度、符号和锚点权重受到限制。

### 3.13 Action 矩阵变化会造成历史数据语义漂移

当 LLM 修改矩阵后，只保存 Action 系数不足以解释旧实验，因为相同系数可能已经映射成不同参数配置。

最终增加 Trace 编码，但不增加新的模型：

- 保存每次实验实际编译出的最终参数；
- 保存其相对 `x_base` 的真实参数增量；
- 数值、整数、布尔和枚举分别采用确定的编码；
- F 模型使用可复现的实际配置变化作为内部输入依据。

这样，LLM 可以更新矩阵，而历史实验仍然保持可解释和可训练。

### 3.14 Metric 数量和职责曾过于复杂

早期方案同时保留 TTFT、TPOT、Throughput 等多个相互相关指标，容易重复建模。

最终保留 6 个中间 Metric：

1. KV Cache 使用率 P95；
2. 抢占率；
3. GPU 平均利用率；
4. 队列长度 P95；
5. GPU 显存 I/O 平均利用率；
6. TTFT P95。

最终输出只保留 `output_token_throughput_tps`。TPOT 不再作为独立节点，避免与吞吐重复表达同一性能趋势。

### 3.15 图结构和矩阵的作用曾混在一起

两者现在明确分工：

- Action–Metric 图：决定一个 Metric 能调用哪些 Action；
- Action 参数矩阵：决定每个 Action 如何改变 15 个 vLLM 参数；
- F 模型：学习这些 Action 对对应 Metric 的实际影响；
- G 模型：学习各 Metric 与最终吞吐之间的关系。

图负责“选谁”，矩阵负责“怎么改”，模型负责“实际效果如何”。

---

## 4. 已删除或大幅简化的冗余内容

### 4.1 Docker 和独立 Engine 容器

删除内容：

- 为每个推理引擎建立单独容器；
- Docker 镜像构建与编排；
- 容器级资源隔离；
- 容器启动、探活和回收逻辑。

当前方案使用一个统一 Python/CUDA/vLLM 环境，以本地进程启动引擎。

### 4.2 工厂级分布式调度

删除内容：

- 双 Worker 或多 Worker 调度；
- 分布式任务租约；
- NFS 共享状态；
- Exactly-once 执行保证；
- 跨节点协调和锁；
- 复杂任务状态机。

这些功能对验证算法核心思想没有直接帮助。

### 4.3 故障注入和完整故障恢复

删除内容：

- 人为注入引擎崩溃；
- 网络中断模拟；
- Worker 失联恢复；
- 自动续跑和断点重放；
- 复杂的重试、回滚和补偿流程。

当前只保留最基本的超时、失败记录和跳过非法候选，足以支持原型实验。

### 4.4 复杂预算与提案生命周期

删除或合并：

- 独立 BudgetLedger；
- 多级预算审批；
- proposal、accepted、scheduled、leased、committed 等过细状态；
- 多阶段候选审核和人工门禁。

当前只需要记录已执行实验数，并根据总预算停止搜索。

### 4.5 过度复杂的可观测性和运行手册

删除或压缩：

- 面向生产环境的告警体系；
- 多层 SLO 和服务等级管理；
- 大量风险表、人工审批表和恢复手册；
- 与算法验证无关的长期运维设计。

保留必要内容：实验配置、运行状态、Metric、吞吐、失败原因和模型版本。

### 4.6 过大的模块拆分

原方案把配置、预算、调度、执行、恢复、审计、提案、模型和报告拆成大量微模块，模块之间接口数量过多。

最终收缩为围绕以下核心职责的模块：

- 参数与 Action 定义；
- 图结构；
- 候选配置编译；
- vLLM 运行；
- 指标采集；
- Trace 存储与编码；
- F/G 模型；
- Metric 选择；
- 动态 Action 集合构建；
- BO 优化；
- LLM Action 更新；
- 主循环与结果输出。

### 4.7 过量测试

原方案的测试规模接近生产平台，包括大量故障注入、恢复和并发一致性测试。

最终测试重点改为：

- 每个核心模块的单元测试；
- 配置编译、图选择、模型输入输出和主循环的接口测试；
- 初始化阶段与 BO 闭环的端到端测试。

大致从数十个生产级测试案例收缩为 13 组模块测试、4 组接口测试和 2 组端到端测试。

### 4.8 过长的实施周期

原计划大约需要 22 天，并包含很多生产化工作。

删除外围内容后，当前计划压缩为约 14 天，重点是先跑通算法闭环，而不是提前建设完整调参平台。

---

## 5. 当前最终保留的核心流程

### 阶段一：初始化

1. 建立统一运行环境；
2. 定义 15 个 vLLM 参数及其类型、范围、步长和约束；
3. 初始化 8 个具有明确语义的 Action 向量；
4. 初始化完整 Action–Metric 图；
5. 执行 5～10 次初始实验；
6. 用初始数据拟合 F 和 G；
7. 选择成功实验中吞吐最高的配置作为固定 `x_base`。

### 阶段二：每轮选择优化对象

1. 检查具有明确正常方向的 Metric 是否超过高要求阈值；
2. 明显异常的 Metric 直接进入候选集；
3. 如果没有明显异常，则用 G 的反事实重要性分析选择最影响吞吐的 Metric；
4. 每轮选择 1～3 个 Metric，默认 2 个；
5. 从图中取得这些 Metric 连接的全部 Action，并求并集；
6. 未进入并集的 Action 系数严格设为 0。

### 阶段三：动态维度贝叶斯优化

1. 只为本轮 Action 并集建立搜索变量；
2. 使用 F 预测这些 Action 对已选 Metric 的影响；
3. 搜索使 Metric 更接近正常范围的 Action 系数组合；
4. 将 Action 组合映射为 15 维参数增量；
5. 所有候选均从固定 `x_base` 生成；
6. 对整数执行四舍五入，对分类参数执行加权投票；
7. 检查范围与组合约束，得到最终 vLLM 配置；
8. 执行实验并记录真实配置、Metric 和吞吐。

### 阶段四：在线更新

1. 每个 BO 小轮次默认执行 3 次实验；
2. 小轮次内保持 Action 矩阵不变；
3. 用新实验更新 F 和 G；
4. 由 LLM 分析预测误差和实际变化；
5. LLM 只小幅修改本轮探索过的 Action 向量；
6. 下一小轮次重新选择 Metric 和 Action，并继续优化；
7. 达到实验预算后输出最佳配置和完整 Trace。

---

## 6. 原方案与当前方案对比

| 对比项 | 原方案 | 当前方案 |
| --- | --- | --- |
| 系统目标 | 同时建设完整调参平台和生产运行系统 | 先验证 DIBO 核心算法 |
| 运行环境 | Docker、独立 Engine、可能多 Worker | 单机统一环境、本地进程 |
| 模型结构 | F、G、H 或额外局部 GP | 仅 F 和 G |
| Metric 选择 | 规则不清或直接检测瓶颈 | 高阈值优先，无异常时使用 G 重要性 |
| Action 选择 | 固定 Top-2 或固定数量 | 已选 Metric 的全部邻接 Action 并集 |
| 搜索维度 | 固定 | 每轮动态变化 |
| 未选 Action | 可能仍参与或由 Mask 控制 | 系数严格为 0 |
| 参数状态 | Active Mask、冻结、能力状态并存 | 无 Mask、无冻结，统一约束检查 |
| 候选基准 | 可能逐轮累加 | 始终相对固定 `x_base` 生成 |
| 分类参数 | 可能错误地做数值加法 | 加权投票 |
| LLM 更新 | 太晚或每次实验都更新 | 每个 BO 小轮次后小幅更新 |
| 数据一致性 | 矩阵变化后旧系数含义漂移 | 保存真实编译配置和相对基础增量 |
| 工程范围 | 分布式、容错、故障注入、运维 | 核心调参闭环 |
| 计划周期 | 约 22 天 | 约 14 天 |

---

## 7. 当前方案仍需注意的限制

这些不是结构性错误，但在实现和实验时需要留意。

### 7.1 初始样本较少

5～10 个初始化实验对于最多 8 维的动态搜索仍然偏少。它适合先验证系统能否工作，但 F 的早期不确定性会较大。

建议：正式实现默认使用 10 个初始点，并保留以后增加样本预算的配置项。

### 7.2 多个 Metric 的邻接并集可能覆盖全部 Action

如果一次选择 3 个连接范围较广的 Metric，Action 并集可能接近 8 个，动态降维效果会变弱。

建议：默认只选 2 个 Metric；只有多个 Metric 同时严重越界时才增加到 3 个。

### 7.3 部分 Action 语义可能相关

例如序列并发、批 Token 预算和 Chunked Prefill 都可能同时影响 GPU 利用率、队列和 TTFT，因此不同 Action 的实验效果可能相关。

建议：先保留可解释的 Action 定义，等积累实验后再根据相关性决定是否合并，而不是现在提前删除。

### 7.4 G 表示统计关系，不等于严格因果关系

G 可以分析哪些 Metric 的变化最可能改善吞吐，但不能自动证明因果。

建议：把 G 的结果称为“模型重要性”或“反事实敏感度”，不要直接表述为确定的因果瓶颈。

### 7.5 LLM 不应过于频繁地改矩阵

矩阵变化太快会让 BO 难以积累稳定经验。

建议：只在 BO 小轮次边界更新，并限制单元格最大变化量、锚点符号和总体范围。

### 7.6 不冻结参数可能消耗少量无效实验

完全不冻结符合当前简化目标，但某些参数在特定硬件或 vLLM 版本中可能没有明显作用。

建议：当前只记录“是否实际生效”和最终编译值，不引入新的冻结系统；如果未来实验明确证明长期无效，再考虑简化 Action 定义。

---

## 8. 最终评价

原始工程计划覆盖面很完整，但更像一个面向生产环境的自动调参平台，其中包含了大量与当前研究原型无关的调度、容错、审计、恢复和部署工作。这些内容会显著增加开发量，却不能更快验证 DIBO 的核心思路。

经过多轮修改后，当前方案已经基本对齐目标：

- 用初始实验建立 F/G 模型；
- 以最佳初始实验作为固定基础配置；
- 先选择关键 Metric，再由图结构动态决定全部相关 Action；
- 只在这些 Action 上进行贝叶斯优化；
- 让 Metric 回到正常区间，并通过 G 关联最终吞吐；
- 让 LLM 根据实验结果逐小轮修正 Action 先验；
- 不使用 Active Mask、不冻结参数，也不建设暂时不需要的生产级外围系统。

因此，当前版本比原方案更适合作为第一阶段代码实现：结构更小、算法关系更清楚、每个模块都能被单独验证，也更容易在完成核心闭环后再决定是否增加生产化能力。

